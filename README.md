# Whale Trader Dashboard

Mobile-friendly read-only dashboard for the whale-trader bot.

Bot on VPS pushes `data.json` snapshots every 2 minutes.
Static site served via GitHub Pages.

Access: token-protected URL fragment.
